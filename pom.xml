<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.cognizant.logitrack</groupId>
    <artifactId>logitrack-microservices</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>logitrack-microservices</name>
    <description>LogiTrack Microservices Migration</description>

    <modules>
        <!-- Infrastructure -->
        <module>service-registry</module>
        <module>config-server</module>
        <module>api-gateway</module>
        <!-- Services -->
        <module>identity-access-service</module>
        <module>supplier-po-service</module>
        <module>warehouse-inventory-service</module>
        <module>route-carrier-service</module>
        <module>shipment-freight-service</module>
        <module>compliance-doc-service</module>
        <module>notification-alert-service</module>
        <module>logistics-analytics-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.0</spring-cloud.version>
        <lombok.version>1.18.34</lombok.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
