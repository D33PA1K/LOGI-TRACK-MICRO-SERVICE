server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/2507291/Downloads/microservices_migration bd/microservices_migration/config-repo

management:
  endpoints:
    web:
      exposure:
        include: health,info