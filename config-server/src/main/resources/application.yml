server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:\\\C:\Users\2507295\OneDrive - Cognizant\Documents\LOGITRACK-BACKEND-AND-FRONTEND\LOGI-TRACK-MICRO-SERVICE\config-repo
management:
  endpoints:
    web:
      exposure:
        include: health,info